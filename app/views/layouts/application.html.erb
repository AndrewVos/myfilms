<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>myfil.ms</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">
            myfil.ms
          </a>
        </div>

        <div class="collapse navbar-collapse" id="navbar">
          <p class="navbar-text">
            the community-curated film ratings site
          </p>
            <ul class="nav navbar-nav">
              <% if signed_in? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">My lists <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <%= active_link_to "Watched", watched_lists_path, wrap_tag: :li %>
                    <%= active_link_to "Want to watch", want_to_watch_lists_path, wrap_tag: :li %>
                  </ul>
                </li>
                <%= active_link_to "Recommendations", recommendations_path, wrap_tag: :li %>
              <% end %>
            </ul>
          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to 'Contact', 'https://twitter.com/AndrewVos' %>
            </li>
            <% if signed_in? %>
              <%= active_link_to "Account", edit_user_registration_path, wrap_tag: :li %>
              <%= active_link_to "Sign Out", destroy_user_session_path, wrap_tag: :li, method: :delete %>
            <% else %>
              <%= active_link_to "Sign In", new_user_session_path, wrap_tag: :li %>
              <%= active_link_to "Sign Up", new_user_registration_path, wrap_tag: :li %>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>
