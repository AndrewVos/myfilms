<div class="container-fluid">
  <div class="row">
    <div class="col-xs-12">
      <% @movies.each do |movie| %>
        <%= render 'movies/movie', movie: movie %>
      <% end %>
      <%= paginate @movies %>
    </div>
  </div>
</div>
