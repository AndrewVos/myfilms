(function () {
  var tmdbId = '<%= @tmdb_id %>'
  var $movie = $('.movie[data-tmdb-id="' + tmdbId + '"]')
  var $span = $movie.find('.want-to-watch button span')
  $span.toggleClass('glyphicon-check')
  $span.toggleClass('glyphicon-unchecked')
})()
